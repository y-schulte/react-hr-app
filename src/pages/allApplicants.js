import React from "react";

//this page is not being used for current project:
const allApplicants = () => {
  return (
    <div>
      We can add more components here to display and sort applicant's data
    </div>
  );
};

export default allApplicants;
